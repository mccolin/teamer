# Teamer

Microsoft Teams keepalive script. On random intervals, activate Teams
and push it to focus, while preventing computer from sleeping.

Use:

```sh
./teamer.sh
```

Also included is Keeper, a simple wrapper around `caffeinate` which outputs
progress reminders while it prevents your machine from sleeping until Ctrl-C
broken.

```sh
./keeper.sh
```


